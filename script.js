// Check if the page is loaded
document.addEventListener("DOMContentLoaded", () => {
  console.log("Storefront is ready.");

  // Example: Alert user when they add an item (Optional)
  /* document.addEventListener('snipcart.events.item.adding', (item) => {
        console.log(`${item.name} is being added to cart`);
    }); 
    */
});
